# Backend - Supabase Integration

This folder contains all backend-related code, services, and utilities for the Supabase integration.

## Structure

- `services/` — Data access layer (CRUD operations per table)
- `hooks/` — React hooks wrapping backend services
- `types/` — Backend-specific TypeScript types
- `utils/` — Helper utilities (auth, validation, etc.)
